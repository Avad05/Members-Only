<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Members Only</title>
    <link rel="stylesheet" href="/assets/style.css">
</head>
<body class="bg-[#030712]">
    <div class="flex align-middle justify-between m-3 border-b border-amber-50 p-2">
    <div class="flex">
    <img src="/uploads/image.png" class="size-12 shrink-0">
    <header class="text-white underline text-4xl">Members Only</header>
    </div>
    <div class="flex gap-5">
    <a href="/login"><button class="bg-[#432e89] text-white text-2xl p-2 rounded-md">Login</button></a>
    <a href="/sign-up"><button class="text-[#432e89] bg-white text-2xl p-2 rounded-md">Sign-Up</button></a>
    </div>
    </div>
    <div class="flex flex-col justify-items-center place-items-center m-8 p-5 gap-4">
         
    <% messages.forEach(element => { %>
        <div class="flex gap-4 border-white border rounded-xl w-xl h-auto p-4">
            <img src="/uploads/message.png" class="size-8 shrink-0">
            <div>
            <strong class="text-white font-extrabold text-sm"><%= element.displayName %></strong>
            <br>
           <p class="text-white text-lg"><%= element.message %></p> 
            <br>
            <small class="text-white text-xs"><%= element.created_at.toLocaleString() %></small>
            <br>
            </div>
        </div>
    <% }) %>

</div>
</body>
</html>